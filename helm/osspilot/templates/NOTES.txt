OssPilot {{ .Chart.Version }}（镜像 tag {{ include "osspilot.imageTag" . }}）

  租户 API   {{ include "osspilot.fullname" . }}-tenant-api:{{ .Values.tenant.api.service.port }}
  运营 API   {{ include "osspilot.fullname" . }}-ops-api:{{ .Values.ops.api.service.port }}
  租户 Web   {{ include "osspilot.fullname" . }}-tenant-web:{{ .Values.tenant.web.service.port }}
  运营 Web   {{ include "osspilot.fullname" . }}-ops-web:{{ .Values.ops.web.service.port }}

Worker 与对应 API 同一镜像，command 为 worker。migrate 是 pre-install/pre-upgrade Job。

S3 / RGW 在运营端系统设置填写，不写进本 chart。
ceph-mgmt 未单独部署；需要时设 ops.api.cephMgmtApiUrl。

{{- if .Values.tenant.api.ingress.enabled }}
租户 API Ingress: https://{{ .Values.tenant.api.ingress.host }}
{{- end }}
{{- if .Values.tenant.web.ingress.enabled }}
租户控制台 Ingress: https://{{ .Values.tenant.web.ingress.host }}
{{- end }}
{{- if .Values.ops.api.ingress.enabled }}
运营 API Ingress: https://{{ .Values.ops.api.ingress.host }}
{{- end }}
{{- if .Values.ops.web.ingress.enabled }}
运营控制台 Ingress: https://{{ .Values.ops.web.ingress.host }}
{{- end }}
