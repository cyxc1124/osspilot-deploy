# 复制为 values-local.yaml 或 values-production.yaml 后改。不要提交真实密钥。

global:
  imageTag: "v0.1.0"
  imagePullSecrets:
    - name: ghcr-pull-token

jwt:
  secret: "replace-me"
  accessTokenExpireMinutes: 60
projection:
  secret: "replace-me"

database:
  url: postgres://osspilot:replace-me@ops-postgres:5432/osspilot_ops?sslmode=require
tenantApiUrl: http://osspilot-tenant-api:8000

# 集群内探活地址，一般不用改。
about:
  tenantWorkerUrl: http://osspilot-tenant-worker:8080
  tenantWebUrl: http://osspilot-tenant-web:8080
  opsWorkerUrl: http://osspilot-ops-worker:8080
  opsWebUrl: http://osspilot-ops-web:8080

ingress:
  enabled: true
  className: traefik
  host: oapi.example.com
  annotations:
    cert-manager.io/cluster-issuer: your-cluster-issuer
  tlsSecretName: osspilot-ops-api-tls
